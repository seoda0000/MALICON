import { createTheme } from "@mui/material";

export const BoldKoreanFont = createTheme({
  typography: {
    fontFamily: "Tenada",
  },
});

export const LightKoreanFont = createTheme({
  typography: {
    fontFamily: "Tenada",
  },
});
